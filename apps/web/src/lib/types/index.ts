// Re-export all shared types for convenient access
export type {
  Vec2,
  EntityId,
  PlayerSnapshot,
  EnemySnapshot,
  BossSnapshot,
  AttackState,
  EnemyAIState,
  BossAIState,
  GameEvent,
} from '@curious/shared';
